let test2 = [];
test2.unshift("ldld");

Math.floor(Math.random() * 10) + 1;


// TODO
function myObject(color, width, fontSize){
    this.color = color;
    this.width = font;
    this.fontSize = fontSize;

    this.newObject = function() {
        console.log(this.password);
    }
}

let objectHTML = '<pre style="' + objectNr1 + '">function myObject(color, width, fontSize){<br/>' +
    'this.color = color;<br/>' +
'    this.width = width;<br/>' +
'    this.fontSize = fontSize;<br/>' +
'    <br/>' +
'    this.newObject = function() {<br/>' +
'        console.log(this.password);<br/>' +
'    }<br/>' +
'})<br/></pre>';

let objectNr1 = new myObject("coral", "150px", "15px");

objectNr1.getPassword();

high = document.getElementsByClassName('highlight');
high[9].innerHTML = objectNr1;

// Retrieve the domain name
window.location.hostname;

// Save and then retrieve some data with localStorage
window.localStorage.setItem('name', 'reinis');
window.localStorage.getItem('name')

// Add some cookies
document.cookie = "username=reinis";

// Create a list, loop thru the list elements and add a class to each of the list elements
let allGridItems = document.getElementsByClassName('grid-item');
for (i=0;i<allGridItems.length;i++) {
    allGridItems[i].classList.add('double-item');
}   
// Handle window
window.onload
// Open new window
window.open
// Change it's size
window.resize
// Write data to it
window.innerText
// Add an event listener that triggers window close
window.close
// Create the HTML (css if needed) and add three different event listeners to created HTMLd
document.body.innerHTML = "<div>something new</div>";
myDiv.addEventListener('click', myDiv.write("something"));
myDiv.addEventListener('click', myDiv.write("something"));
myDiv.addEventListener('resize', myDiv.write("something"));
